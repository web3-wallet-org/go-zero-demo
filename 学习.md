# go-zero 钱包 REST Demo 学习步骤

这个 demo 的目标是按 go-zero 的 Quick Start 流程，做一个小型钱包 REST 服务，重点学习 `.api`、`goctl` 代码生成、handler、logic、svc、配置和接口测试。

## 总体流程

| 步骤 | 内容 | 状态 |
| --- | --- | --- |
| 第 1 步 | 安装 `goctl` | 已完成 |
| 第 2 步 | 初始化 Go module | 已完成 |
| 第 3 步 | 编写 `wallet.api` | 已完成 |
| 第 4 步 | 用 `goctl api go` 生成 go-zero 工程 | 已完成 |
| 第 5 步 | 添加内存钱包存储 `WalletStore` | 已完成 |
| 第 6 步 | 实现 logic 层业务代码 | 已完成 |
| 第 7 步 | 整理依赖并编译检查 | 待执行 |
| 第 8 步 | 启动 go-zero 服务 | 待执行 |
| 第 9 步 | 用 HTTP 请求测试接口 | 待执行 |

## 第 1 步：安装 goctl

命令：

```powershell
$env:GOPROXY = 'https://goproxy.cn/,direct'
go install github.com/zeromicro/go-zero/tools/goctl@latest
C:\Users\AAB\go\bin\goctl.exe --version
```

学习点：

- `goctl` 是 go-zero 的代码生成工具。
- `go install ...@latest` 会把命令安装到 `GOPATH\bin`。
- 当前环境中安装路径是 `C:\Users\AAB\go\bin\goctl.exe`。

## 第 2 步：初始化 Go module

命令：

```powershell
go mod init github.com/web3-wallet-org/go-zero-demo
```

学习点：

- `go.mod` 是 Go 项目的模块和依赖声明文件。
- module 名使用仓库路径，方便后续包引用保持一致。

## 第 3 步：编写 wallet.api

文件：

```text
wallet.api
```

学习点：

- `.api` 是 go-zero 特有的 REST API 描述文件。
- `type` 用来定义请求和响应结构。
- `service` 用来定义 HTTP 路由。
- `@handler` 指定生成的 handler 名称。
- `@server(group: wallet)` 会让生成代码进入 `wallet` 分组目录。

当前接口：

```text
POST /wallets
GET /wallets/:address
POST /wallets/transfer
```

## 第 4 步：生成 go-zero 工程

命令：

```powershell
C:\Users\AAB\go\bin\goctl.exe api go -api wallet.api -dir .
```

学习点：

- `goctl` 会根据 `wallet.api` 生成 REST 工程骨架。
- 生成的主要目录包括：

```text
etc/
internal/config/
internal/handler/
internal/logic/
internal/svc/
internal/types/
```

核心分层：

```text
wallet.api
  -> types
  -> handler
  -> logic
  -> svc
```

## 第 5 步：添加内存钱包存储

文件：

```text
internal/store/walletstore.go
internal/svc/servicecontext.go
```

学习点：

- `WalletStore` 使用 `map[string]int64` 保存钱包余额。
- 使用 `sync.RWMutex` 保证并发安全。
- `ServiceContext` 是 go-zero 推荐的依赖注入位置。
- logic 层通过 `l.svcCtx.WalletStore` 访问共享依赖。

已实现方法：

```go
Create(address string, balance int64) (Wallet, error)
Get(address string) (Wallet, error)
Transfer(from, to string, amount int64) (Wallet, Wallet, error)
```

## 第 6 步：实现 logic 层业务代码

文件：

```text
internal/logic/wallet/createwalletlogic.go
internal/logic/wallet/getwalletlogic.go
internal/logic/wallet/transferlogic.go
internal/logic/wallet/walletmapper.go
```

学习点：

- handler 负责 HTTP 参数解析。
- logic 负责业务处理。
- svc 负责提供依赖。
- types 负责 API 请求和响应结构。

请求处理链路：

```text
HTTP 请求
  -> handler 解析参数
  -> logic 调用 ServiceContext 中的依赖
  -> 返回 types 响应结构
  -> handler 输出 JSON
```

## 第 7 步：整理依赖并编译检查

命令：

```powershell
go mod tidy
go test ./...
```

学习点：

- `go mod tidy` 会整理 `go.mod` 和 `go.sum`。
- `go test ./...` 会测试并编译当前目录及所有子目录的 Go package。
- 即使没有测试文件，这个命令也可以检查语法、类型和 import 是否正确。

## 第 8 步：启动服务

命令：

```powershell
go run wallet.go -f etc/wallet-api.yaml
```

学习点：

- `wallet.go` 是服务入口。
- `-f` 指定配置文件。
- `etc/wallet-api.yaml` 配置服务名、监听地址和端口。

当前配置：

```yaml
Name: wallet-api
Host: 0.0.0.0
Port: 8888
```

启动流程：

```text
读取 yaml 配置
  -> 加载到 config.Config
  -> 创建 rest server
  -> 注册 handler 路由
  -> 启动服务
```

## 第 9 步：测试接口

创建钱包：

```powershell
curl -i -X POST http://localhost:8888/wallets `
  -H "Content-Type: application/json" `
  -d '{"address":"alice","balance":1000}'
```

创建另一个钱包：

```powershell
curl -i -X POST http://localhost:8888/wallets `
  -H "Content-Type: application/json" `
  -d '{"address":"bob","balance":100}'
```

查询钱包：

```powershell
curl -i http://localhost:8888/wallets/alice
```

转账：

```powershell
curl -i -X POST http://localhost:8888/wallets/transfer `
  -H "Content-Type: application/json" `
  -d '{"from":"alice","to":"bob","amount":200}'
```

再次查询：

```powershell
curl -i http://localhost:8888/wallets/alice
curl -i http://localhost:8888/wallets/bob
```

预期结果：

```text
alice: 800
bob: 300
```

## 后续可继续学习

- 给 store 添加单元测试。
- 给 logic 添加单元测试。
- 增加 MySQL model。
- 增加 Redis cache。
- 增加 JWT 鉴权。
- 增加 middleware。
- 拆成 API + RPC 服务。
